# Tests

Integration and system tests for the MediBill application.

Backend tests are located in `backend/tests/`
Frontend tests are located in `frontend/`

Run all tests:
```bash
./run_tests.sh
```
